---
import Layout from '~/layouts/BaseLayout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Error 404 Not Found`;
---

<Layout meta={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Error</span>
          <span class="bg-clip-text text-transparent bg-gradient-to-r from-primary-500 to-secondary-500">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">抱歉，目前查無此頁。</p>
        <p class="mt-4 mb-8 text-lg text-gray-600 dark:text-slate-400">請點擊下方連結回到首頁！</p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">返回首頁</a>
      </div>
    </div>
  </section>
</Layout>
